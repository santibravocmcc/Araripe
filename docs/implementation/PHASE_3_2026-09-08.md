# Phase 3 — congelar e ensaiar o replay de 2026

**Data:** 2026-09-08
**Base verificada:** `origin/main` em
`c39c5238d8e939f6a37bc9060ea5c9ec6da8bd3e` (`git rev-parse origin/main`)
**Site:** `origin/main` em `99805f6` — forma curta de propósito, porque o hook
`commit-msg` deste repositório recusa um SHA de 40 caracteres que não exista
*aqui*, inclusive o SHA legítimo do site
**Branch:** `claude/phase3-freeze`
**Suite:** 1593 na base → **1723** neste ramo, todos passando

> **Adendo de 2026-09-09 — leia a §10.** A PR `#56` foi mesclada, o dono decidiu
> a baseline do replay (**`2.1.0`**) e mediu a cota de `ee-araripe`. As duas
> pendências que este documento deixou abertas fecharam, e **uma afirmação da §3
> deste documento estava errada** — o número está corrigido na §10.

---

## 1. O estado do exit gate P3

> *"The small rehearsal is reproducible, completeness checks pass, rollback
> works, and the full replay can run without mutating the live release."*

| cláusula | estado | prova |
| --- | --- | --- |
| o ensaio limitado é reprodutível | **FECHADA** | duas execuções independentes dão a mesma `rehearsal_fingerprint`; e um passo alterado muda a impressão, então o digest mede algo |
| as checagens de completude passam | **FECHADA** | `verify_release` executado nos quatro passos; a release anterior verifica completa depois da falha e depois da drenagem |
| a reversão funciona | **FECHADA** | ponteiro 1 → 2 → **3 (rollback)** → 4, com o alvo revalidado inteiro antes de mover |
| o replay inteiro pode rodar sem mutar a release viva | **FECHADA com uma ressalva medida** | o ensaio só escreve em `araripe-v2-staging` e nenhuma chave sob `alerts/`, `baselines/`, `data/timeseries/` ou `persistence_state` foi escrita; a ressalva é a §4 |

Os bullets do roadmap, um por um:

| bullet | estado |
| --- | --- |
| escolher e registrar a data de corte, e enfileirar o resto | **FEITO** como regra + data provisória `2026-08-30` + fila explícita |
| manter os agendamentos azuis rodando, isolar o verde | **RESPEITADO** — nada foi disparado, nada azul foi tocado |
| fotografia com checksum dos sete assuntos | **FEITO**, com dois assuntos declarados não medidos e a razão |
| congelar as dez versões | **FEITO** como pin medido, não lista em prosa |
| estimar cota, lotes, storage, transferência e runtime | **FEITO** nos insumos, **com uma correção medida** — §3 |
| ensaiar staging, falha, reversão e data enfileirada | **FEITO**, executado |
| revisão explícita do dono antes do cutover | **PENDENTE — é ação do dono** |

## 2. As duas ativações de runtime

### 2.1 Baseline 2.1.0 — carregável, e a inércia do azul provada

`src/detection/baseline_selection.py` é um registro **fechado** das gerações
carregáveis com resolvedor que falha fechado: sem variável de ambiente, sem
varredura de diretório, sem "mais nova ganha", sem fallback. O default vem de
`config.settings.BASELINE_VERSION`, que **não foi tocado**.

Os três `run_detection*` ganharam `--baseline-version` (default `None`) e
passaram a gravar na persistência a versão **resolvida** em vez da constante
importada. Sem isso, um replay contra a 2.1.0 gravaria `1.0.0` em cada track.

`run_detection_gee.py` resolve **antes** de qualquer chamada ao Earth Engine —
um `--baseline-version` digitado errado antes falharia só depois de baixar
todos os compostos da janela.

**Mutação derrubada:** `BASELINE_VERSION = "2.1.0"` em `config/settings.py`
derruba três testes em dois arquivos.

**O que a comparação mede** (dos dois manifests, sem baixar 13 GB):

- mesma grade, mesmo extent, mesmos 72 nomes de arquivo;
- **zero** rasters em comum (interseção de SHA-256 = 0/72);
- `valid_fraction` da 2.1.0 maior nos **72/72** objetos, +0,51 a +0,82 pp;
- procedência: v1 `partial` (IDs de cena, task IDs e os 12 checksums de export
  **não** retidos) contra v2.1 `complete`;
- a 2.1.0 traz contrato de composição, plataformas observadas com política
  fail-closed, dispersão populacional em float64 e profundidade de contribuição
  por índice e mês.

**A recomendação técnica é a 2.1.0, e a decisão é do dono.** O runbook §2.1 traz
o custo por inteiro: a 2.1.0 admite produtos pré-Collection-1 nos meses 1-4, que
a ESA está reprocessando, então um replay contra ela **pode ter de ser refeito
para janeiro-abril**.

**Também medido, e é material da decisão:** os rasters da 2.1.0 estão em disco
(13 GB) e os **72 casam byte a byte** com o manifest. Os da 1.0.0 **não estão
locais** — a produção os busca do R2 em tempo de execução — então a
comparação pixel a pixel entre as duas gerações **não foi feita** e não pode ser
feita nesta máquina sem baixar da produção.

### 2.2 Metadado de datatake no export — portado, e um achado

`scripts/build_detection_gee.py` passou a enumerar os **datatakes físicos** da
janela: uma chamada `reduceColumns` pelas `SCENE_PROPERTIES`, agrupamento por
`(normalize_platform(SPACECRAFT_NAME), DATATAKE_IDENTIFIER)`, e o instante lido
do **identificador** do datatake e nunca de `system:time_start`, que é o
instante do grânulo.

Documento novo `araripe_detection_run_manifest_v3.json`, **aditivo**: o
`araripe_detection_acquisitions.json` que `load_composite_acquisition` consome
ficou intacto em forma.

`import ee` e `ee.Initialize` foram para dentro de `main()`, o que torna a
derivação **importável e testável** — antes o corpo do módulo inicializava o
Earth Engine e nada dali tinha teste.

**O achado, e ele é da Phase 4.** Um primeiro rascunho pré-computava cada
`acquisition_id`. Os valores **não** batiam com os que `CompositionRunV3`
deriva: aquela classe amarra `composite_method_id` a
`coverage-ranked-first-valid-v1` — composição escopada por **datatake**
(`src/detection/composition_run_v3.py:189`) — enquanto o export mosaica a
**data** inteira sob `daily_mosaic-v1`. Logo o export enumera e deixa a
identidade ser cunhada por quem compõe. Um ID plausível que não casa é pior que
nenhum ID.

**Mutação derrubada:** derivar o instante do campo do grânulo. A checagem
cruzada da própria biblioteca a pega —
`src/detection/composition_run_v3.py:133`: *"declared acquisition timestamp
2026-08-30T13:08:42Z disagrees with the sensing instant embedded in datatake
GS2A_20260830T130251_012345_N05.11"*.

**Verificado de novo nesta sessão:** `grep -rn build_detection_gee .github/` não
devolve nada. Nenhum workflow roda o script, então editá-lo não é mudança de
runtime azul.

## 3. Uma correção medida na estimativa de cota

`docs/operations/PHASE_3_INPUTS_2026-09-08.md` §1 estima o custo do replay
partindo de `SEARCH_DAYS_BACK = 16`. **Medido: o valor é 5**
(`config/settings.py:53`, e o mesmo em `ed5f913`, então não é regressão desta
sessão). Duas docstrings ainda dizem 16 — `scripts/r2_state.py:49` e
`tests/test_update_tracks.py:222` — e foram deixadas como estão de propósito:
elas descrevem o incidente de 2026-09-07, quando a janela **era** 16.

A aritmética corrigida, com as mesmas premissas do documento original:

- ~8,6 execuções/mês × 5 dias ≈ **43** processamentos-de-data por mês, não 138;
- custo por processamento-de-data ≈ **X/43**;
- ~242 datas numa passagem ≈ 242 × X/43 ≈ **5,6 X**, não 1,75 X.

**A conclusão do documento sobrevive: a cota não limita.** Se a detecção
consumir algo próximo dos 3,41% que a reconstrução da baseline mostrou, o replay
fica em ~**19% de um mês** de alocação, e dois replays em ~38%. **O que não
sobrevive é a margem declarada:** o documento diz *"mesmo com uma margem de erro
de 10× continua caber num mês"*, e 10 × 19% > 100%.

`config/phase3_replay_freeze_v1.json` grava
`algorithm.incremental_search_days_back = 5` do produtor, e um teste cai se ele
mudar — para a próxima estimativa partir do número certo.

**Continua sendo ação do dono** abrir a página de cotas de `ee-araripe` e
registrar duas linhas: o limite mensal e o uso atual. É a única medição que
falta para a estimativa deixar de ter um X.

## 4. O que continua sem prova

Esta é a seção que importa para a Phase 4.

1. **A cadeia GEE inteira não foi exercitada.** Nenhuma chamada ao Earth Engine
   foi feita: `ee.Initialize()` ignora `GOOGLE_APPLICATION_CREDENTIALS` e esta
   máquina não tem credencial de serviço. A enumeração de datatakes está
   validada contra propriedades de cena **gravadas**, e a paridade com a
   biblioteca está provada; o que **não** está provado é que
   `reduceColumns(Reducer.toList(7, 1), …)` devolve as sete colunas nesta ordem
   para esta coleção. **A primeira execução real da Phase 4 deve começar por uma
   janela de poucos dias e conferir o manifest antes de enfileirar 242 datas.**
2. **O inventário de alertas no R2 não foi medido.** A fotografia o declara não
   medido, com a razão: nenhuma credencial, e produção é somente-leitura nas
   Fases 2B-5. Rodar `scripts/snapshot_replay_freeze.py --r2-read` de um
   ambiente com chave de leitura o preenche. O caminho está escrito e **não foi
   executado**.
3. **`data/persistence_state.geojson` não existe localmente.** A fotografia o
   declara não medido. O estado vivo está no R2 e é buscado por
   `scripts/r2_state.py get`.
4. **A comparação pixel a pixel entre 1.0.0 e 2.1.0 não foi feita** (§2.1). A
   decisão da baseline se apoia nos manifests, que são checksum do inventário e
   das estatísticas por objeto — não em diferença de pixel.
5. **O ensaio não rodou contra o R2 real nesta fase.** Ele rodou contra um store
   que impõe as precondições do R2 e que **pode falhar quando mandado**, o que a
   execução real não pode. As provas contra o R2 real do gate P2B estão em
   `PHASE_2B_GATE_2026-09-08.md`; o que é novo aqui é a *sequência*
   falha→retentativa→reversão→drenagem, e ela é provável dessa forma e não da
   outra.
6. **A unidade de composição do replay não está decidida** (§2.2). É decisão da
   Phase 4, com as duas opções já no código.
7. **A grade do export não está fixada.** Ele pede só `crs` e `scale`, sem
   `crsTransform`, então a origem é escolha do Earth Engine e o alinhamento com
   a grade da baseline **não** está prometido. A evidência de que ninguém o
   assumiu é o `reindex_like(..., tolerance=15)` em
   `scripts/run_detection_from_gee.py`. Fixar a transform mudaria pixels
   exportados: Phase 4.
8. **A revisão do dono está pendente** (runbook §7). Sem ela o cutover não
   começa, e ela inclui a decisão da baseline.
9. **A fila registrada tem 0 datas**, porque a última data do banco de série
   temporal É a data provisória do corte. Ela cresce enquanto as Fases 4 e 5
   correm; a Phase 4 grava a sua própria fila com o corte resolvido.

## 5. O que esta fase deliberadamente não fez

- **Não escolheu a baseline do replay.** `replay_generation.decided = false`.
- **Não rodou a Phase 4**, nem começou as Fases 5, 6 ou 7.
- **Não tocou produção**: nenhum workflow disparado, nenhuma escrita no R2,
  nenhuma credencial usada, `detect_gee.yml` e `update_data.yml` intocados.
- **Não fixou a data de corte como literal** — a regra resolve na hora, e fixar
  hoje acrescentaria semanas de fila sem ganho.
- **Não congelou a taxonomia de rótulos qualificada**, que é da Phase 5 e tem
  portão de revisão do dono.
- **Não mudou `config/settings.py`.**

## 6. Os artefatos desta fase

| arquivo | o que é | digest |
| --- | --- | --- |
| `config/phase3_replay_freeze_v1.json` | as dez versões congeladas, lidas dos produtores | `freeze_sha256` `3554cf715af82cc80be591ddc115e5390a2f2e1bd39894b7e1a6f31b6a481fa3` |
| `docs/implementation/PHASE_3_SNAPSHOT_2026-08-30.json` | a fotografia dos sete assuntos | `snapshot_sha256` no próprio arquivo |
| `docs/implementation/PHASE_3_POST_CUTOFF_QUEUE_2026-08-30.json` | a fila pós-corte, explícita | `queue_sha256` `0706482b25f57a27af2321a72b382d155e51e27ba2df276087acee370d29ebd4` |
| `docs/operations/PHASE_3_REPLAY_RUNBOOK.md` | o procedimento e os alvos resolvidos | revisão do dono **pendente** |

O `snapshot_sha256` não é citado aqui de propósito: a fotografia contém o `HEAD`
do backend, e o commit que a adiciona é filho daquele `HEAD`. Um documento não
pode conter o próprio commit; o valor está no arquivo, e o `HEAD` que ele grava
é o do commit imediatamente anterior.

## 7. Armadilhas pagas nesta sessão

- **Um teste pode passar pelo motivo errado — de novo, e mediu-se a lacuna.**
  `test_nenhum_script_verde_importa_o_carregador_de_dotenv` proibia `config` e
  `dotenv` nos cinco scripts verdes olhando só o AST do **próprio** script.
  Medido: com `from src.detection.baseline_selection import resolve_baseline`
  acrescentado a `scripts/plan_retention.py`, aquele teste **passa**. O guarda
  transitivo novo falha e nomeia a cadeia inteira. A mutação foi revertida.
- **`FakeS3.__init__` copia o dict de objetos.** Construir o store de
  retentativa com `objects=fake.objects` lhe dava um retrato de **antes** de
  qualquer publicação, e a primeira promoção falhava com "release.json is
  absent" — um erro que pareceria do `atomic_publish` e era do arnês.
- **Duas canonicalizações não são a mesma.** Medido: `json.dumps(sort_keys=True,
  separators=(",",":"))` e a forma RFC 8785 da biblioteca discordam em float de
  valor integral (`1.0` vs `1`) e em notação de expoente (`1e-07` vs `1e-7`);
  concordam nos bounds do AOI por acidente de valor. Por isso o manifest do
  export e o congelamento **recusam float** e gravam decimal em texto: as duas
  formas passam a concordar para qualquer documento, e não só para os tentados.
- **`cd` composto no shell das ferramentas pega, e persiste.** Um
  `cd ../site && …` mudou o diretório de trabalho da sessão e a execução
  seguinte da ferramenta rodou o arquivo errado. Caminho absoluto.
- **Caminho absoluto num documento commitado.** A primeira fotografia gravava
  `/Users/sbravo/...` nas razões de "não medido". Além de o `AGENTS.md` proibir,
  isso faria duas fotografias da mesma árvore diferirem por quem as tirou.

---

## 10. Adendo de 2026-09-09 — a decisão, a cota, e uma correção da §3

**Suite:** 1723 na `main` mesclada → **1738** com este adendo.

Escrito no dia seguinte, depois de a PR `#56` ser mesclada em `origin/main`
(`baea4f7326caca198f61a2a640909010b0070111`, **merge commit** — os cinco commits
preservados) e a `#25` do site ser squash-mergeada (`5304a81`). As duas
confirmadas por conteúdo, não por ancestralidade.

### 10.1 A baseline do replay está decidida: `2.1.0`

Perguntado *"qual referência o recálculo usa — a antiga ou a nova"*, o dono
respondeu **"a nova"**, seguindo a recomendação com o custo declarado.

Registrada em `config/phase3_replay_baseline_decision_v1.json`, com autorização
datada, e o congelamento **lê** a decisão dali em vez de a repetir.
`build_freeze` a valida: a versão nomeada tem de ser uma geração **registrada**
(um erro de digitação, um nome retirado ou a `2.0.0` superada falham fechado), a
autorização tem de existir com `authorized_by: project_owner` e data, e o
documento tem de declarar `blue_default_change_permitted: false`.

**A inércia do azul continua provada, agora contra um caso novo:** decidir a
`2.1.0` para o replay **não** moveu `BASELINE_VERSION`, que segue `1.0.0`.
`test_decidir_o_replay_nao_move_o_default_do_azul` exige que os dois valores
continuem diferentes e ambos gravados no congelamento.

Novo `freeze_sha256`:
`e877e9b1b1150d64c5ac71aeaf7bb23ff7c8b59b29984bd301bfdc3f8611a577` (era
`3554cf715af82cc80be591ddc115e5390a2f2e1bd39894b7e1a6f31b6a481fa3`; a única
diferença é o bloco `baseline.replay_generation`).

### 10.2 A cota está medida, e o X deixou de ser um X

| projeto | limite mensal | uso | % |
| --- | --- | --- | --- |
| **`ee-araripe`** (detecção e replay) | 3.600.000 EECU-s | **6.242** | **0,17%** |
| `ee-araripe-baseline-v2` (baseline) | 3.600.000 EECU-s | 122.783 | 3,41% |

Medido de `gh run list --workflow detect_gee.yml`: os 6.242 vêm de **duas**
execuções completas (2026-09-03 agendada e o dispatch de 2026-09-07). A agendada
de 2026-09-07 **falhou sem gastar compute** — `gh run view 34120627779` mostra a
parada em *"Fetch persistence state from R2"* e o passo *"Run GEE detection"*
como `skipped`.

    3.121 EECU-s por execucao  x  8 execucoes/mes  =  X = 24.968 EECU-s/mes = 0,69%

Aritmética completa e as ressalvas em
`docs/operations/PHASE_3_INPUTS_2026-09-08.md` §3. Resultado: **uma passagem do
replay ~115.800 EECU-s = 3,2% de um mês**; com erro de 10×, 32% — ainda cabe.
Confirmação independente: **0,94×** o custo da reconstrução da baseline v2.

### 10.3 A correção: a §3 deste documento publicou uma magnitude errada

A §3 corrigiu a estimativa dos insumos de `SEARCH_DAYS_BACK = 16` para `5`, e
**essa parte estava certa**. Mas ela então converteu `5,6 X` em *"~19% de um
mês"* usando **o consumo do projeto `ee-araripe-baseline-v2` como proxy do
consumo da detecção**. Medido agora, esse proxy era **4,9× alto**: a detecção
consome 0,69% ao mês, não 3,41%.

Consequências, ditas por inteiro:

- o número certo é **~3,2%**, não ~19%;
- **a frase que a §3 declarou morta volta a valer.** A §1 dos insumos dizia
  *"mesmo com uma margem de erro de 10× continua caber num mês"*; a §3 escreveu
  que isso não sobrevivia, porque 10 × 19% > 100%. Com 32%, sobrevive;
- a §3 **não** é reescrita. Ela registra o que se sabia em 08/09, e apagar um
  erro publicado tira do próximo leitor a razão pela qual o número está pinado.

A lição, e ela é o motivo de o pin existir: **um proxy não medido merece a mesma
desconfiança que um número stale.** A §3 acertou ao desconfiar do `16` e errou ao
não desconfiar igualmente do proxy que usou para substituir o `X`. O
congelamento agora grava `algorithm.incremental_search_days_back` do produtor, e
a §3 dos insumos grava o `X` medido — as duas com teste.

### 10.4 O estado do que continuava sem prova

Da lista de nove da §4:

| item | estado em 09/09 |
| --- | --- |
| 1. cadeia GEE não exercitada | **continua** — nenhuma credencial de serviço aqui |
| 2. inventário de alertas no R2 não medido | **continua** — o caminho existe e não foi executado |
| 3. `persistence_state.geojson` não local | **continua** |
| 4. comparação pixel a pixel entre gerações | **continua** — a decisão se apoia nos manifests |
| 5. ensaio não rodou contra o R2 real | **continua**, e é deliberado |
| 6. unidade de composição não decidida | **continua** — é da Phase 4, agora a decisão mais consequente dela |
| 7. grade do export não fixada | **continua** — é da Phase 4 |
| 8. revisão do dono pendente | **parcial** — 2 de 6 itens fecharam; o resto é **pré-cutover**, portão da Phase 6 |
| 9. fila registrada com 0 datas | **continua** — a Phase 4 grava a sua com o corte resolvido |

### 10.5 Uma armadilha paga neste adendo

**Um teste meu passou pelo motivo errado, e só a mutação revelou.**
`test_o_runbook_registra_a_decisao_da_baseline_que_o_congelamento_le` afirmava
`f"--baseline-version {version}" in text`. Medido: trocando o comando do
procedimento da §3 do runbook para `--baseline-version 1.0.0`, o teste
**passava** — porque a §2.1 do runbook menciona `--baseline-version 2.1.0`
noutra linha, e a asserção só exigia que a string existisse em algum lugar.

Corrigido para exigir que **toda** ocorrência de `--baseline-version` no runbook
nomeie a geração decidida (`set(re.findall(...)) == {version}`), e a mutação
passou a cair com `{'1.0.0', '2.1.0'} == {'2.1.0'}`. É a terceira instância do
mesmo padrão nesta linha de trabalho, e a segunda em que o teste defeituoso era
meu.

**Também escrito e corrigido:** o arquivo de decisão nasceu com um `sha256` de
manifest **inventado** — plausível, com 64 caracteres hexadecimais, e falso. Foi
substituído pelo valor de `shasum -a 256 config/baseline_manifest_v2_1.json`
(`c012924ab8be3d55d7f1d2f632900a5362ad4402c543ed01c08b1a08da2eba29`) e um teste
confere que ele bate com o que o congelamento lê do mesmo arquivo. É exatamente
o defeito que o `AGENTS.md` descreve: *"um identificador plausível é pior que um
obviamente ausente, porque a revisão não o pega"*.
