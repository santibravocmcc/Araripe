# Package 2B.0 — isolated green foundation

**Started:** 2026-08-11

**Status:** Partially implemented; both separate R2 staging identities verified
live (staging-only allow, production denied), inert v2 lanes drafted on a local
review branch, Worker Builds audited, and an unrouted staging Worker created;
GitHub Environment installation awaits renewed local GitHub authentication

**Production impact:** None

## Purpose

Package 2B.0 makes blue/green isolation explicit before any state, workflow,
publication, Worker, or site migration. The current blue system remains the
production fallback through Phases 2B–5. Green components use distinct
credentials, resources, workflows, and deployment targets and cannot promote a
canonical release until Phase 6.

## Live Cloudflare mutation performed

The connected Cloudflare capability created one additive R2 bucket:

- account: `9416750169311ee4afc18a8ff3c771d4`;
- bucket: `araripe-v2-staging`;
- creation time: `2026-08-11T17:34:56.082Z`;
- location: Eastern Europe (`EEUR`);
- storage class: Standard.

Post-creation read-back confirmed:

- managed `r2.dev` access disabled;
- no custom domain;
- no CORS policy;
- no object-lock rules;
- default seven-day incomplete-multipart abort rule only.

No object was uploaded. No existing bucket, Worker, route, DNS record, Worker
Build, binding, GitHub secret, workflow, release pointer, or public artifact was
changed. In particular, `araripe-cogs`, the production Worker, and
`observatoriodachapadadoararipe.com` were untouched.

## Credential decision

Claude Code receives only an R2 S3 `Object Read & Write` credential scoped to
the exact bucket `araripe-v2-staging`. It must not receive
`CLOUDFLARE_API_TOKEN` in the production account. The user must create the R2
credential once in the Cloudflare dashboard and save it as the local AWS profile
`araripe-r2-staging`; the one-time secret must never enter an agent transcript.

Exact setup and acceptance tests are in
`docs/operations/CLOUDFLARE_STAGING_ACCESS_FOR_CLAUDE.md`.
AWS CLI v2.36.20 was installed locally through Homebrew and verified on
2026-08-11. Both later-created profiles use region `auto`; the R2 account URL
is an endpoint supplied separately, not an AWS region.

The bucket credential permits object operations only. Bucket configuration,
Workers, routes, DNS, Builds, bindings, token creation, and promotion remain
Codex/Cloudflare-control-plane tasks. A missing capability invokes the
versioned `araripe-safe-handoff` skill rather than a broader credential.
The current resume checkpoint is
`docs/handoffs/20260811T221716Z_phase2b0-control-plane.md`; the earlier
credential checkpoints remain immutable audit history.

## Cross-tool recoverability implemented

The canonical skill is stored at
`.agents/skills/araripe-safe-handoff/SKILL.md` and exposed to Claude by the thin
adapter `.claude/skills/araripe-safe-handoff/SKILL.md`. It requires:

- target/capability classification before mutation;
- exact credential and environment preflight;
- immutable writes and no partial pointer/ledger promotion;
- a durable, exclusive checkpoint under `docs/handoffs/` when blocked;
- explicit user notice naming the missing capability;
- a self-contained Codex handoff prompt;
- branch/commit/cloud-state revalidation before resume.

The helper accepts only bounded, owner-only mode-`0600` JSON under
`/private/tmp`; rejects duplicate keys, common/opaque secret forms in payload or
Git branch identity, unsafe controls, and symlink escapes; neutralizes inline
HTML/Markdown; and stops Git capture at the configured bound. Suspicious opaque
Git path components are replaced with labelled SHA-256 digests, with only the
exact expected checkpoint path exempted. It records repository state
immediately before installation and the checkpoint path as the exact expected
delta. A complete temporary copy is fsynced and installed once
through an exclusive hard link; an existing target is never replaced, and the
directory is fsynced after installation.

## Blue resources that remain unchanged

- backend schedules and manual fallback on the production default branch;
- site data-refresh schedule and existing bot-generated Git data;
- `araripe-cogs` and its fixed legacy prefixes/state objects;
- production Worker `observatorio-chapada`;
- Worker Builds integration from the site repository;
- final apex domain and routes.

This is intentional. The candidate backend branch already contains scientific
changes, including the five-day window. Merging it wholesale would make the old
scheduled workflow run candidate science against production state. That is
forbidden.

## Credential verification performed (2026-08-11, resumed session)

The user created the bucket-limited R2 credential and stored it as AWS profile
`araripe-r2-staging` outside both repositories. A resumed Claude session then
verified, without reading or printing any secret value:

- AWS CLI v2.36.20 present; profile `araripe-r2-staging` listed;
  `~/.aws/credentials` is mode `600`;
- `aws s3 ls s3://araripe-v2-staging` succeeds with the profile against the
  account endpoint;
- the same list against `s3://araripe-cogs` fails with `AccessDenied`
  (ListObjectsV2), as required;
- one put/get/delete smoke test under the unique prefix
  `credential-smoke-test/12731b04-7a63-44db-89cd-a07d0d6a1bae/` passed with a
  SHA-256 round-trip match, and the bucket was left empty;
- read-only HEAD of `https://observatoriodachapadadoararipe.com` returned
  HTTP 200 via Cloudflare — the production Worker and domain are untouched.

Capability probes in the same session: `wrangler` is installed but
unauthenticated and the Cloudflare MCP connectors are unauthorized — Claude has
no Cloudflare control-plane path, which is the intended boundary. `gh` is
authenticated with user scope (`repo`, `workflow`).

## Inert green lanes drafted (2026-08-11, local branch)

The local review branch `claude/phase2b0-green-isolation` (from `4e2e9ee`)
adds the inert manual v2 route and the three-lane concurrency design:

- `.github/workflows/v2_candidate_replay.yml` — green candidate lane
  `araripe-green-candidate`; `workflow_dispatch` only; `contents: read`;
  GitHub Environment `v2-staging`; fails closed unless the environment names
  exactly `araripe-v2-staging` and the approved endpoint; probes staging
  allow/production `AccessDenied`; writes only one immutable per-run object.
- `.github/workflows/v2_promotion_lane.yml` — serialized promotion placeholder
  `araripe-green-promotion` with `cancel-in-progress: false`; no environment,
  secrets, or R2 access; bounded hold input for the post-merge lane proof.
- `docs/operations/GREEN_CONCURRENCY_LANES.md` — defines the legacy-live,
  green-candidate/replay, and serialized-promotion lanes, the distinctness
  argument, and the post-merge proof procedure.

The branch is local only. Nothing is pushed or merged; the v2 route cannot run
until it reaches the default branch through a reviewed merge, and even then it
is dispatch-only and fails closed without the `v2-staging` environment.

## Cloudflare control-plane resume (2026-08-11)

Codex revalidated the checkpoint and the live account before mutation. The
production Worker remained `observatorio-chapada`, with custom domain
`observatoriodachapadadoararipe.com`, rate-limit namespace `1001`, no cron
trigger, and the same deployment metadata recorded before this resume.

The Worker Builds audit proved that candidate site branches are **not safe to
push under the current configuration**:

- the repository connection is `santibravocmcc/observatorio-site`;
- production builds watch `main` and run `npx wrangler deploy`;
- a second active trigger watches every branch except `main` and also runs
  `npx wrangler deploy`;
- that custom non-production command differs from Cloudflare's safe preview
  default (`npx wrangler versions upload`) and targets the repository's default
  Worker name unless a separate staging configuration is made explicit.

No trigger was changed and no site branch was pushed. Candidate site work must
remain local until a reviewed staging-only config and deploy command are
approved together.

Codex then created one additive Worker named
`observatorio-chapada-v2-staging`. Read-back proved:

- R2 binding `STAGING_BUCKET` targets only `araripe-v2-staging`;
- rate-limit binding `STAGING_LIMITER` uses namespace `2001`, distinct from
  production namespace `1001`;
- managed `workers.dev` and preview URLs are disabled;
- no custom domain, zone route, cron schedule, asset bundle, AI binding, or
  secret is attached;
- the Worker contains only an inert health/404 placeholder and cannot publish
  site or release data.

The immutable Cloudflare before-state is recorded in
`docs/operations/CLOUDFLARE_GREEN_BEFORE_STATE_2026-08-11.md`.

## GitHub staging identity verification (2026-08-11)

The user created the separate Actions identity as local AWS profile
`araripe-r2-github-staging`. Without reading or printing either secret, Codex
confirmed that the credentials file remains mode `600`, corrected both local
R2 profiles from an endpoint mistakenly entered as the default region to
region `auto`, and verified that the new identity can list
`araripe-v2-staging` while `araripe-cogs` returns `AccessDenied`.

Environment installation stopped before mutation because the local GitHub CLI
session is no longer authenticated and the connected GitHub capability does
not expose Environment secret management. The user must renew `gh`
authentication; the R2 identity must not be pasted into chat or reused as a
broader GitHub token.

## Restricted Cloudflare broker draft (2026-08-12)

A local, inactive broker now provides a future Claude control-plane path
without giving Claude a direct Cloudflare credential:

- `.github/workflows/cloudflare_green_control.yml` is manual-only,
  default-branch-only, serialized, and bound to protected Environment
  `cloudflare-green-control`;
- `scripts/cloudflare_green_control.py` accepts only three named operations and
  fixes every account, zone, Worker, bucket, trigger, repository, and command;
- the only Worker Builds mutation makes the wildcard non-production deploy
  command inert (`exit 0`); it does not execute branch-controlled Wrangler
  configuration;
- no route/domain/DNS, delete, production deploy, generic URL/body/command, or
  secret retrieval operation exists;
- every mutation requires `GREEN-ONLY` and ends with a full isolation audit;
- `.github/CODEOWNERS` assigns the broker and instruction boundary to the
  repository owner, and focused regression tests enforce the allowlist and
  fail-closed target checks.

The broker token will remain only in GitHub Environment secrets. The draft is
not pushed, merged, installed, approved, or dispatched. Exact setup and the
rule for a later separate R2-admin broker are documented in
`docs/operations/RESTRICTED_CLOUDFLARE_BROKER.md`.

## Protected GitHub Environment installation (2026-08-13)

Renewed local GitHub CLI authentication was verified as `santibravocmcc`
(scopes `repo`, `workflow`; ADMIN on `santibravocmcc/Araripe` and
`santibravocmcc/observatorio-site`). Both repositories matched the
`20260812T204106Z` checkpoint exactly; the only backend delta was the user's
commit installing that checkpoint file itself.

The user created both GitHub Environments in `santibravocmcc/Araripe`, each
with required reviewer `santibravocmcc` and deployment restricted to branch
`main`:

- `cloudflare-green-control` — the user directly installed secret
  `CLOUDFLARE_GREEN_CONTROL_TOKEN` and the documented variables
  `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_ZONE_ID`, and
  `CLOUDFLARE_CONTROL_SCOPE=green-only`. Claude verified environment
  protection, secret name, and variable names/values only; the credential was
  never retrieved, read, or replaced.
- `v2-staging` — after re-proving live that local profile
  `araripe-r2-github-staging` lists `araripe-v2-staging` and receives
  `AccessDenied` on `araripe-cogs`, the identity was installed by direct
  non-printing pipe transfer into secrets `R2_STAGING_ACCESS_KEY_ID` and
  `R2_STAGING_SECRET_ACCESS_KEY`. Variables `R2_STAGING_BUCKET`,
  `R2_ENDPOINT_URL`, and `AWS_REGION=auto` match
  `docs/operations/CLOUDFLARE_STAGING_ACCESS_FOR_CLAUDE.md` exactly. No secret
  value was read, printed, or stored.

Session re-verification: the 35 focused broker/handoff tests and the full
387-test backend gate pass; the production domain answers HTTP 200 via
Cloudflare; neither local review branch exists on either `origin`.

One divergence needs an owner decision before the protected merge: the active
`main` ruleset ("Protect main — pull requests only") requires a pull request
and blocks deletion and force pushes with no bypass actors, but
`require_code_owner_review` is disabled and zero approving reviews are
required — weaker than the CODEOWNER approval described in
`docs/operations/RESTRICTED_CLOUDFLARE_BROKER.md`. GitHub forbids
self-approval, so in this single-maintainer repository enabling those
requirements without a bypass actor would deadlock owner-authored pull
requests. The owner must choose the enforcement/bypass combination
deliberately; until then the pull-request gate remains procedural.

## Remaining Package 2B.0 work

1. ~~User creates and locally stores the one-bucket Claude R2 credential.~~
   Done 2026-08-11.
2. ~~Verify allowed list/put/get/delete in `araripe-v2-staging` and denied
   access to `araripe-cogs` without revealing values.~~ Done 2026-08-11.
3. ~~Re-audit current Cloudflare Worker Builds branch behavior before any site
   candidate-branch push.~~ Done 2026-08-11. The wildcard non-production
   trigger uses `npx wrangler deploy` and is unsafe for candidate pushes; no
   branch was pushed and no trigger was changed.
4. ~~Define a distinct staging Worker/environment with no apex/custom route and
   its own R2 binding/rate-limit namespace.~~ Done 2026-08-11. The isolated,
   unrouted placeholder Worker is `observatorio-chapada-v2-staging`, bound only
   to `araripe-v2-staging` with rate-limit namespace `2001`.
5. ~~Create separate GitHub Environment `v2-staging` and a different
   bucket-only Actions identity; do not reuse Claude's identity.~~ Done
   2026-08-13. The protected Environment exists (required reviewer,
   `main`-only deployment) and holds the separate identity's secrets and the
   documented variables together; bucket scope re-verified live before the
   non-printing transfer.
6. Introduce new v2 workflows in an inert/manual route that cannot be triggered
   by existing production schedules. **Drafted locally on
   `claude/phase2b0-green-isolation`; needs review, merge, and post-merge
   inertness confirmation.**
7. Define three concurrency lanes: legacy live mutation, green candidate/replay,
   and serialized green staging-pointer promotion. **Defined in
   `docs/operations/GREEN_CONCURRENCY_LANES.md`; runnable distinctness proof
   pending the reviewed merge.**
8. Record an accepted before-state after the live recheck and pass the 2B.0 gate.
9. Add and review an explicit site staging configuration, then replace the
   wildcard non-production deploy command with a staging-only command. This
   must be approved before changing the existing Worker Builds trigger or
   pushing any site candidate branch.
10. Protect, review, and merge the restricted Cloudflare green broker; install
    its separately scoped token only in the protected GitHub Environment and
    prove `audit` before allowing Claude to request a mutation. **The user
    installed the token and variables in protected Environment
    `cloudflare-green-control` on 2026-08-13. Still open: the owner's `main`
    ruleset code-owner decision, the reviewed merge, and the post-merge
    `audit` proof; `disable-site-branch-deploy` needs its own separate
    approval.**

No new permanent repository is planned. Isolation uses branches, workflows,
GitHub Environments, credentials, buckets/namespaces, and a staging Worker. A
temporary private orchestration repository is permitted only if the existing
Cloudflare/GitHub default-branch integration cannot provide demonstrably inert
manual execution.

## Exit gate

Package 2B.0 closes only when:

- Claude's token succeeds on the staging bucket and is denied on production;
- site non-production branch behavior is proven safe;
- the staging Worker has no production route;
- v2 workflows cannot run on a schedule or mutate legacy resources;
- green credentials have no blue/public-promotion authority;
- the safe-handoff skill passes structural and execution tests in both tool
  discovery paths.

Until then, only read-only design and non-mutating local preparation for 2B.1
may proceed. No 2B.1 implementation, remote activation, or gate claim is
allowed before 2B.0 closes.
